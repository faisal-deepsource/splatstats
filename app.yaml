runtime: python38

instance_class: F4

handlers:
# This configures Google App Engine to serve the files in the app's static
# directory.
- url: /static
  static_dir: static/

automatic_scaling:
  min_instances: 0
  max_instances: 1
